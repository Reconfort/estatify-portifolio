import { TenantsPage } from "@estatify/feature-tenant-admin";

/** /tenants — Access Management · Tenants. Feature lives in @estatify/feature-tenant-admin. */
export default function Page() {
  return <TenantsPage />;
}
