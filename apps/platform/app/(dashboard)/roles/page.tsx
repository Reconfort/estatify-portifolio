import { RolesPage } from "@estatify/feature-tenant-admin";

/** /roles — Access Management · Roles (with the grouped permission builder). */
export default function Page() {
  return <RolesPage />;
}
