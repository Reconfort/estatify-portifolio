import { PermissionsPage } from "@estatify/feature-tenant-admin";

/** /permissions — Access Management · Permissions (read-only catalog). */
export default function Page() {
  return <PermissionsPage />;
}
