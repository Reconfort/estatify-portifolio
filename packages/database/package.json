{
  "name": "@estatify/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "prisma:generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "seed": "tsx src/seed.ts",
    "rbac:sync": "tsx src/rbac-seed.ts",
    "studio": "prisma studio"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@estatify/types": "workspace:*",
    "@prisma/client": "^6.2.0",
    "argon2": "^0.41.1"
  },
  "devDependencies": {
    "prisma": "^6.2.0",
    "tsx": "^4.19.2"
  },
  "nx": {
    "name": "database",
    "tags": [
      "scope:api",
      "type:data-access"
    ]
  }
}
