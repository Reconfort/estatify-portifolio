/**
 * @estatify/media — reusable, non-branded media shared across applications.
 *
 * Owns: default avatar, property placeholder, empty-state graphics (empty
 * search / empty dashboard), loading graphics, generic defaults. NOT branding,
 * NOT tenant uploads (those live in object storage, never in Git).
 *
 * Import via alias: `import { PropertyPlaceholder } from "@estatify/media"`.
 * Add assets under src/ and re-export them here. Tags: scope:shared, type:ui.
 */
export {};
