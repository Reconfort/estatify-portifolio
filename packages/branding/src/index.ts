/**
 * @estatify/branding — ESTATIFY PLATFORM branding only.
 *
 * Owns: logos, brand marks, favicons, OpenGraph/social images, official
 * illustrations, brand placeholders. NOT application assets, NOT tenant assets.
 *
 * Export assets as typed modules (SVG-as-component or static import path) so
 * apps import via alias: `import { EstatifyLogo } from "@estatify/branding"`.
 * Add assets under src/ and re-export them here. Tags: scope:shared, type:ui.
 */
export {};
