# pnpm configuration — single source of truth for the workspace package manager.
# Peers are auto-installed so shadcn/Radix/Next peer graphs resolve cleanly.
auto-install-peers=true
# Do not hard-fail the whole install on a peer mismatch; surface as warnings.
strict-peer-dependencies=false
# Deterministic dependency resolution.
resolution-mode=highest
